package backend.model;

public enum ShadowType {
    NONE,               // Sin sombra
    SIMPLE,             // Sombra simple (gris)
    COLORED,            // Sombra coloreada (mismo color oscurecido)
    SIMPLE_INVERSE,     // Sombra simple inversa (gris negativo)
    COLORED_INVERSE     // Sombra coloreada inversa
}
